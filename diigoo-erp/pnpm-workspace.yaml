packages:
  - "apps/*"
  - "packages/*"

# unrs-resolver ships an optional native binary used only by ESLint's import
# resolver. We don't need it for typecheck/build, so skip its build script.
allowBuilds:
  unrs-resolver: false
